# 更新日志

## v1.3.5（2026-08-11）

### 品牌与发布
- 正式更名为「满木工具箱」，窗口标题、侧边栏、软件信息、EXE 属性全部同步
- EXE 图标更换为满木品牌图标（resources/app.ico，多尺寸）
- 体积优化：剔除用不到的 Qt 模块（QML/Quick/虚拟键盘/Pdf/Network），EXE 150MB → 141MB
- 新增 Inno Setup 中文安装程序：自动创建开始菜单/桌面快捷方式，带卸载入口，免 UAC

### 小白友好交互
- 整页拖放：把文件直接拖到工具页任意位置，自动匹配第一个能处理该格式的工具并选中、填入文件，无需先点卡片再拖
- 任务完成后自动显示「打开目录」按钮，一键直达输出文件夹
- 面板左上角标题用强调色、右上角收起按钮用强调色描边，操作区文字与按钮有了清晰的颜色区分
- 工具卡片悬停时轻微上浮（阴影），一眼就能看出卡片可点击；改为瞬时切换，彻底消除内容"抽动"
- 操作面板顶部新增拖拽手柄：鼠标悬浮显示上下拖动光标，自由调整面板显示大小，尺寸自动记忆（下次打开恢复）

### 软件信息
- 「关于」升级为独立的软件信息窗口，向下滚动可查看完整更新日志
- 新增作者信息：博客园（alloverzyt）一键访问、QQ 一键复制

### 视觉细节打磨
- 依赖管理页 Tab 改为 Material 下划线式，不再有凸起胶囊遮挡 Tab 下方的分割线
- 底部抽屉面板四边内收 20px 内边距，适配圆角，左上角文字与右上角按钮不再顶到边框
- 面板内部控件不再左右顶满，观感更透气

## v1.3（2026-08-11）

### UI 体验优化
- 操作面板新增底部常驻「展开操作区 ▲」入口按钮，收起状态下也能一眼找到操作区；展开后标题栏出现「收起操作区 ▼」
- 面板展开/收拢改为三路并行动画：面板高度 + 透明度渐变 + 卡片区高度同步伸缩，消除原先卡片区"瞬移"的生硬感
- 工具少的分类页自动适配布局：
  - 3 个以内 → 单列居中大卡片（图标 48px、大字号、限宽 560）
  - 4-6 个 → 两列大卡片
  - 6 个以上 → 紧凑两列
  - 卡片内容垂直居中，页面不再空旷
- 工具卡片随布局自动放大/缩小组件尺寸，视觉效果统一

### 其他
- 满木 Logo 去除白色背景，侧边栏与应用图标更协调
- 时间戳转换、颜色转换、二维码生成三个纯参数工具不再强制要求拖入文件，设置参数后直接可运行
- 依赖管理页重构为「核心 / 可选 / 外部」三个 Tab，可选依赖支持单个安装

### 体验修复
- 操作面板改为「底部抽屉」：面板不透明、最多占页面 88% 高度、内容超高可滚动，运行按钮固定底部
- 「收起操作区」按钮提升到窗口右上角（窗口层级浮动，不随页面滚动），面板展开时出现、收起或切页时消失
- 大卡片统一等宽等高：单列卡片放入限宽容器填满，不再出现卡片大小不一

## v1.2（2026-08-10）

### UI 体验优化
- 全新黑色简笔画图标体系（Lucide，深色/浅色模式自动适配），替换彩色 emoji 图标
- Material Design 深色模式分层配色，莫奈柔和暖米白浅色模式
- 主题调色盘：10 组莫奈预设色 + 自定义强调色，背景层级随强调色联动
- 工具卡片布局：2 列卡片网格、悬停实色调和色（修复浅色模式"阴间"悬停）
- 页面统一使用滚动容器，参数区/文件区设置最小高度，控件不再被压缩
- 依赖管理改为卡片列表展示
- 首次启动欢迎向导，自动配置依赖

### 功能
- 33 个工具 · 7 大分类（PDF / 图片 / 文档 / 文件 / 二维码 / 媒体 / 实用）
- PDF 无损转图片（DPI 可调）、合并拆分、压缩、水印、加解密
- 图片格式转换 / 按目标大小压缩（二分搜索 + 质量下限保护）/ EXIF 读取
- PPT 转图片（LibreOffice 无头模式）、文档转 PDF
- 二维码生成（文本 / WiFi / vCard / 联系人）、识别
- 视频转 GIF、音频提取（FFmpeg）
- 文件批量重命名 / 查重 / 哈希 / 智能归档
- 时间戳转换 / 颜色转换 / Base64 等实用工具
