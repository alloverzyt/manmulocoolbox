# 更新日志

## v1.3.9（2026-08-13）

### 修复打包版「安装依赖全部失败」（关键）
- **根因一：pip 被排除出包**：LocalToolbox.spec 的 excludes 里曾列入 `pip`/`setuptools`/`wheel`，导致打包版里 `sys.executable -m pip install` 找不到 pip，任何 Python 依赖都装不上。已将三者移入 hiddenimports 确保入包
- **根因二：子进程 Python 解释器启动失败**：PyInstaller onefile 的 bootloader 会把 `PYTHONHOME` 指向 `_MEI` 解压目录，应用内再次运行 EXE（pip 安装）时，子进程继承该变量后在自己的临时目录里找不到 stdlib，报「Python path configuration」错误直接闪退。修复：
  - 应用启动时清理 `PYTHONHOME`/`PYTHONPATH`/`_MEIPASS2` 等环境变量（[src/main.py](src/main.py)）
  - `install_python_package` 为 pip 子进程构造干净环境变量，并加 `CREATE_NO_WINDOW` 避免安装时弹黑窗（[dependency_manager.py](src/core/dependency_manager.py)）
  - 打包脚本同样在构建前清理这些变量，防止污染产物

### 外部工具下载：先检测源，再让用户选
- 点击「下载」后不再盲目按配置顺序挨个试：先并发探测所有下载源的可达性（HEAD 请求，不支持 HEAD 的源自动回退 GET+Range），列出「可用/不可用」让用户自主选择下载哪个源
- 可用源显示绿色、不可用源灰显置灰（含失败原因），默认选中第一个可用源；全部不可用时给出网络提示
- 下载器支持只使用用户选定的源，避免浪费流量反复尝试不可达源

### 打包产物命名
- EXE 文件名规则改为「满木工具箱_版本号.exe」：本次产物为 `满木工具箱_v1.3.9.exe`，文件管理器/下载目录中可直观区分版本，避免与旧版混淆
- 安装脚本的 EXE 文件名宏已改为跟随版本号自动拼接，后续升级只需改 `MyAppVersion`

## v1.3.8（2026-08-13）

### 打包与品牌优化
- EXE 更名为中文「满木工具箱.exe」：不再使用英文名 LocalToolbox.exe，在文件管理器、任务栏、任务管理器中一目了然
- 版本号升级至 1.3.8：EXE 文件属性（版本信息）、软件信息对话框、首次运行配置中的版本号全部同步
- 安装脚本同步更新：Inno Setup 安装包以「满木工具箱.exe」为安装目标，版本号更新为 1.3.8
- 本版本无功能改动，为上一版 v1.3.7 全部修复的最终发布形态

## v1.3.7（2026-08-12）

### 依赖下载全面修复
- 修复「下载卡在 10% 不动」：服务器不返回 Content-Length 时进度永不更新，现已按已下载大小估算推进，并实时显示「百分比 · 已下载/总大小 · 速度」，进度条与状态栏双通道反馈
- 关键 Bug：LibreOffice 三个源的 format 都是 `msi`，但解包分支只认 `msi_tuna`，导致 MSI 下载完无人解包、被直接删除后又被判失败（用户会反复白白下载 355MB）。已修复统一走 `_extract_msi` 解包
- 新增腾讯云镜像作为 LibreOffice 首选源（国内速度快、带宽足），清华 TUNA 因会封禁部分校园/动态 IP 网段降为备选，官方镜像保底；FFmpeg 源重排为 gyan.dev → gh-proxy 代理 BtbN → 直连 GitHub，国内更优
- 已是完整 `.msi` 地址的源跳过目录解析，避免二次请求镜像目录页
- 下载块大小 8192 → 65536 字节，减少 Python 循环开销，大文件下载更快

### 依赖页进度条
- 进度条从 8px 加高到 16px 圆角胶囊并同步全局样式，内部百分比文字不再上下被遮挡，改用下方独立状态标签显示进度与速度

### 依赖缺失拦截（未装依赖不让用工具）
- 新增 `BasePlugin.check_environment()`：LibreOffice/FFmpeg/rembg/pdf2docx 等依赖可检测的插件各自实现
- 选中需要依赖但未安装的工具时：显示警示提示条「此功能需要先安装依赖，请到依赖管理页下载安装」，隐藏拖放区、清空已拖文件、禁用运行按钮、拒绝继续拖入——不会再出现"能拖文件但一运行就报错"的困惑
- 后台 TaskWorker 同样校验环境与输入（双保险，直接调用 API 也拦得住）

### 打包体积优化（301MB → 131MB，后按需回补 pdf2docx）
- 排除 pandas/openpyxl/rembg/onnxruntime 打包（spec excludes）：无人使用或需联网下模型。「AI去背景」在打包版显示红色「未安装」提示（开发版/依赖管理页仍可 pip 安装）
- 资源瘦身：resources 改为按文件逐一收集，跳过 ffplay.exe/ffprobe.exe（各约 140MB，应用只调用 ffmpeg.exe）与源图满木_raw.png
- 内置 FFmpeg 真正可用：`_app_resources_dir()`/`get_download_info`/依赖页外部工具检测均支持 `_MEIPASS` 内置路径——单文件 EXE 双击即可离线使用媒体工具，不再依赖 exe 旁 resources/ 或系统 PATH
- **回补 pdf2docx（PDF转Word 离线可用）**：用户选择牺牲体积换取工具箱稳定性，pdf2docx 及依赖 numpy/cv2 重新打进包（EXE 约 240MB）；pandas/openpyxl 仍排除
- 冻结模式依赖检测改为真实导入：已内置模块显示「已内置」，未内置的 rembg 如实显示「未安装（未内置，仅源码版可用）」

### 依赖缺失提示强化
- 缺失依赖的警示条改为**红色警示**（error 色 + 2px 边框 + 14px 加粗），明显区别于普通提示
- pdf_to_doc/rembg 的 check_environment 增加结果缓存：numpy/opencv 首次导入需数秒，缓存避免每次点卡片卡 UI

## v1.3.6（2026-08-12）

### 全功能实操验证
- 对全部 32 个工具逐一构造真实测试文件（PDF/图片/SVG/文本/JSON/音视频等）并真实调用执行，共 28 个工具实测通过
- 根因修复：`find_ffmpeg` 之前优先使用系统 PATH 里的 ffmpeg，本机会命中被其他软件（如 Trae CN）替换的残缺版，导致「提取音频/音频转换/视频转GIF」全部失败。现在优先使用应用自带的完整 FFmpeg（`resources/ffmpeg/bin/ffmpeg.exe`），PATH 仅作兜底；LibreOffice 查找路径同步修正
- 「提取音频」输出 WAV/OGG 失败的根因：把容器格式名当成了编码器名。已新增格式→编码器映射（wav→pcm_s16le、ogg→libvorbis 等），且 WAV/FLAC 无损编码不再错误附加 `-q:a` 参数，五种输出格式全部验证通过

### Bug 修复
- 修复「音频格式转换」的比特率选项完全无效：UI 上选的 128k/192k/320k 之前根本没传给 ffmpeg，现已真正生效（无损格式如 WAV/FLAC 自动忽略比特率）
- 修复「音频格式转换」源文件与目标同格式时，ffmpeg 边读边写会损坏源文件：输出与输入路径重合时自动追加 `_converted` 后缀
- 修复「视频转GIF」的起始时间/持续时间选项被忽略：现已真正传入 ffmpeg
- 修复「批量压缩」固定报 `unknown file extension: .保持原格式`：输出格式下拉的默认值错写成了界面文案，改为真实取值 `original`
- 修复「批量重命名」目标文件已存在时整批报错（WinError 183）：自动追加序号避让冲突
- 「SVG转PNG」不再依赖 cairosvg（Windows 上需额外安装 cairo DLL，极易失败），改用应用自带的 QtSvg 渲染，开箱即用
- 修复偶发的程序闪退（0xC0000409）：任务完成后直接把 `_worker` 置空会销毁仍在收尾的 QThread 对象，Qt 明确「删除正在运行的 QThread 会导致崩溃」，属于竞态、随机复现。改为先 `wait()` 确认线程真正退出再 `deleteLater`
- 修复「PDF合并」通过界面操作必然失败（永远提示"请至少选择2个PDF文件"）：后台任务原先把所有文件拆成逐个执行，合并类工具一次只能看到 1 个文件。新增插件标记 `process_files_together`，「PDF合并」「重复文件查找」改为一次性接收全部文件

### UI 界面全流程验证（ui_verify_all.py）
- 在真实界面上模拟用户操作逐一点击 32 个工具：点卡片 → 拖入真实测试文件 → 检查拖放反馈 → 点「开始处理」→ 等待完成 → 验证「保持选中」；每工具使用独立沙箱与输出目录互不污染
- 验证结果：28/32 PASS（拖放反馈✓ 保持选中✓ 全部通过）；其余 4 个为依赖未安装——Word转PDF/Excel转PDF/PPT转图片（需 LibreOffice）、去除背景（需 rembg）
- 依赖下载走 UI 验证：点击「依赖管理」页的下载/安装按钮，下载启动、进度、状态显示均正常；实际下载被 Trae 沙箱文件权限拦截（本机环境限制，非应用问题）

### 拖放与连续处理体验
- 拖入/选择文件后，拖放区立即显示「✓ 已添加 N 个文件，可继续拖入或点击按钮追加」绿色提示，标题变为「文件已就绪」，同时操作面板自动滚到参数配置区——窗口再矮也不会误以为文件没拖进去
- 任务完成后不再取消卡片选中、不再收起面板：保持同一工具就绪，直接拖下一个文件即可连续处理，省去每次重新点卡片

## v1.3.5（2026-08-11）

### 品牌与发布
- 正式更名为「满木工具箱」，窗口标题、侧边栏、软件信息、EXE 属性全部同步
- EXE 图标更换为满木品牌图标（resources/app.ico，多尺寸）
- 体积优化：剔除用不到的 Qt 模块（QML/Quick/虚拟键盘/Pdf/Network），EXE 150MB → 141MB
- 新增 Inno Setup 中文安装程序：自动创建开始菜单/桌面快捷方式，带卸载入口，免 UAC

### 小白友好交互
- 整页拖放：把文件直接拖到工具页任意位置，自动匹配第一个能处理该格式的工具并选中、填入文件，无需先点卡片再拖
- 任务完成后自动显示「打开目录」按钮，一键直达输出文件夹
- 面板左上角标题用强调色、右上角收起按钮用强调色描边，操作区文字与按钮有了清晰的颜色区分
- 工具卡片悬停时轻微上浮（阴影），一眼就能看出卡片可点击；改为瞬时切换，彻底消除内容"抽动"
- 操作面板顶部新增拖拽手柄：鼠标悬浮显示上下拖动光标，自由调整面板显示大小，尺寸自动记忆（下次打开恢复）

### 软件信息
- 「关于」升级为独立的软件信息窗口，向下滚动可查看完整更新日志
- 新增作者信息：博客园（alloverzyt）一键访问、QQ 一键复制

### 视觉细节打磨
- 依赖管理页 Tab 改为 Material 下划线式，不再有凸起胶囊遮挡 Tab 下方的分割线
- 底部抽屉面板四边内收 20px 内边距，适配圆角，左上角文字与右上角按钮不再顶到边框
- 面板内部控件不再左右顶满，观感更透气

### Bug 修复
- 修复打包版媒体工具报错「No module named 'src.utils.ffmpeg_helper'」：ffmpeg_helper / libreoffice_helper 在插件函数内部延迟导入，PyInstaller 依赖分析漏收集，已改为 hiddenimports + 目录整体打包双保险
- 修复外部工具下载「所有下载源均失败」：FFmpeg 新增 gh-proxy 镜像（国内可访问 GitHub release）；LibreOffice 新增清华大学 TUNA 镜像（自动解析最新版本，msiexec 静默解包，国内速度快）
- 修复任务失败后界面"莫名消失"、文件"加不进去"：失败时不再 6 秒自动重置清空现场，改为保留错误信息、文件列表和操作面板，可直接点「重试失败」或重新添加文件；仅成功时才自动回到初始界面

## v1.3（2026-08-11）

### UI 体验优化
- 操作面板新增底部常驻「展开操作区 ▲」入口按钮，收起状态下也能一眼找到操作区；展开后标题栏出现「收起操作区 ▼」
- 面板展开/收拢改为三路并行动画：面板高度 + 透明度渐变 + 卡片区高度同步伸缩，消除原先卡片区"瞬移"的生硬感
- 工具少的分类页自动适配布局：
  - 3 个以内 → 单列居中大卡片（图标 48px、大字号、限宽 560）
  - 4-6 个 → 两列大卡片
  - 6 个以上 → 紧凑两列
  - 卡片内容垂直居中，页面不再空旷
- 工具卡片随布局自动放大/缩小组件尺寸，视觉效果统一

### 其他
- 满木 Logo 去除白色背景，侧边栏与应用图标更协调
- 时间戳转换、颜色转换、二维码生成三个纯参数工具不再强制要求拖入文件，设置参数后直接可运行
- 依赖管理页重构为「核心 / 可选 / 外部」三个 Tab，可选依赖支持单个安装

### 体验修复
- 操作面板改为「底部抽屉」：面板不透明、最多占页面 88% 高度、内容超高可滚动，运行按钮固定底部
- 「收起操作区」按钮提升到窗口右上角（窗口层级浮动，不随页面滚动），面板展开时出现、收起或切页时消失
- 大卡片统一等宽等高：单列卡片放入限宽容器填满，不再出现卡片大小不一

## v1.2（2026-08-10）

### UI 体验优化
- 全新黑色简笔画图标体系（Lucide，深色/浅色模式自动适配），替换彩色 emoji 图标
- Material Design 深色模式分层配色，莫奈柔和暖米白浅色模式
- 主题调色盘：10 组莫奈预设色 + 自定义强调色，背景层级随强调色联动
- 工具卡片布局：2 列卡片网格、悬停实色调和色（修复浅色模式"阴间"悬停）
- 页面统一使用滚动容器，参数区/文件区设置最小高度，控件不再被压缩
- 依赖管理改为卡片列表展示
- 首次启动欢迎向导，自动配置依赖

### 功能
- 33 个工具 · 7 大分类（PDF / 图片 / 文档 / 文件 / 二维码 / 媒体 / 实用）
- PDF 无损转图片（DPI 可调）、合并拆分、压缩、水印、加解密
- 图片格式转换 / 按目标大小压缩（二分搜索 + 质量下限保护）/ EXIF 读取
- PPT 转图片（LibreOffice 无头模式）、文档转 PDF
- 二维码生成（文本 / WiFi / vCard / 联系人）、识别
- 视频转 GIF、音频提取（FFmpeg）
- 文件批量重命名 / 查重 / 哈希 / 智能归档
- 时间戳转换 / 颜色转换 / Base64 等实用工具
